{
  "name": "ontology-extractor-frontend",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  }
}
